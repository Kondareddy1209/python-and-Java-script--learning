// WRONG: prompt accepts only ONE argument[Wrong prompt() usage❌ WRONG]

// prompt("Enter the", 5, "characters");


//corrected version
// CORRECT: pass one string (use template literal if needed)
prompt(`Enter 5 characters`);
